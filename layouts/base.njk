<!doctype html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>{{ title }}</title>
  <meta name="description" content="{{ description }}">
  <link rel="stylesheet" href="{{ assets.css }}">

  {% for icon in favicons.html %}
    {{ icon | safe }}
  {% endfor %}
</head>
<body>
  {% block content %}
  {% endblock %}

  <script src="{{ assets.js }}"></script>
</body>
</html>
